# 彭博风格前端重塑设计蓝图（v1 原始版本归档）

> 本文档为先前版本（v1.0）的重构规划，已由 v2.0 规范所取代。为保留历史决策与实现参照，将原内容归档于此。新版规范请参阅 `Bloomberg_Frontend_Redesign.md`。

---

## 1. 概述
- 目标：在不影响后台 API、SSE 推流与既有业务逻辑的前提下，将实时资讯页（`frontend/web/app/news/real-time/page.tsx`）升级为 Bloomberg 风格的高密度面板。  
- 主要功能：实时新闻、AI Chain of Thought、情绪仪表、价格走势、信号列表、多语言切换。  
- 设计参考：初版以白底 + 黑色导航为主（后续在 v2.0 中统一为终端深色方案）。

---

## 2. 体验焦点
1. **信息密度**：新闻、信号、指标在单屏集中呈现；控件使用紧凑排版。  
2. **专业度**：借鉴 Bloomberg 网页版控件（时间粒度按钮、图表工具栏、新闻卡片标注）。  
3. **实时反馈**：LiveStatusBar 展示连接状态，支持超时重试。  
4. **多语言**：`zh-CN` / `en-US` 切换；保留原数据结构不变。  
5. **可进化**：通过 Placeholder 卡片预留市场情报与洞察模块扩展位。

---

## 3. 页面骨架（v1）
```
- 顶部导航条（白底、黑色文字）
- 主体三栏（768px 以上）
  ├─ 左列：待扩展的市场列（占位卡）
  ├─ 中列：实时新闻流 + 信号卡
  └─ 右列：LiveStatusBar、SentimentDial、UpcomingFeatures
- 底部：行情/连接状态条
```
> 断点：`lg` 以下折叠为上下结构，右列抽屉化；`sm` 以下使用 tab 切换。

---

## 4. 视觉（v1）
- 色彩：整体白色面板，导航黑字；按钮 hover 使用浅灰描边。  
- 字体：Inter（正文） + IBM Plex Mono（终端文字）。  
- 阴影：`shadow-[0_8px_16px_rgba(0,0,0,0.15)]`，突出面板层次。  
- 数据颜色：上涨绿色 `#1A8B6D`，下跌红色 `#E23B3B`，中性黄色 `#FFB400`。  
- Badge 样式：圆角 16px，边框颜色对应状态。

---

## 5. 组件拆解
### 5.1 顶部导航
- Logo + 行业导航项（Markets、Economics…）  
- 右侧：Subscribe、用户菜单、语言切换、搜索。  
- 背景白、文字黑；hover 改为 `text-gray-600`。

### 5.2 行情头条区域
- 内容：资产名称、现价、涨跌幅、更新时间。  
- 过滤控件：资产下拉、方向 pill、时间范围按钮。  
- 样式：白底卡片，边框 `border-2 border-border-strong`。

### 5.3 AI 信号列表
- 卡片高度 48px；左侧为 “Buy/Sell + tag”，右侧显示价格与时间。  
- loading/empty/error 状态使用同尺寸骨架或文案。

### 5.4 新闻卡片
- 标题 `text-sm font-semibold`，摘要 `text-xs text-text-secondary`。  
- Tags 使用 `rounded-md border px-2`。  
- hover 上浮 `translateY(-4px)`、阴影加深。

### 5.5 侧栏
- `LiveStatusBar`：展示 `connecting/open/error` 状态。  
- `SentimentDial`：显示情绪得分与置信度。  
- `UpcomingFeatures`：简单列表卡片。

---

## 6. 状态与交互
- **连接异常**：顶部显示黄色 banner，按钮触发 `window.location.reload()`。  
- **新闻交互**：点击打开 `NewsPreviewModal`；`Space/Enter` 触发。  
- **ChainOfThoughtDrawer**：沿用现有抽屉，内容保持逐条 JSON 渲染。  
- **筛选重置**：切换资产时清空搜索与过滤条件。

---

## 7. 实施阶段（v1）
1. 引入白底主题变量、更新 Tailwind 颜色。  
2. 新建 `AppShell` 容器，封装导航 + 三列布局 + 底部 ticker。  
3. 调整 `page.tsx` 使用 `AppShell`，迁移已有组件。  
4. 保留 Placeholder 卡片，后续逐步填充市场与洞察模块。

---

## 8. 兼容性测试
- `npm run lint` / `npm run build` / `npm run test`（待补测试覆盖）。  
- 手动验证多语言切换、SSE 恢复逻辑、窗口缩放断点。

---

## 9. 未来工作（v1 之后）
- 改进 Market Column（关注列表、快速行情）。  
- 重做 SentimentDial UI，支持历史轨迹。  
- 引入顶栏全局搜索联想、比较功能。  
- 替换白底方案为终端黑色主题（已在 v2.0 规范实现）。

---

## 10. 附录
- 相关代码：`app/news/real-time/page.tsx`（v1 实现已集成 `AppShell`）  
- Placeholder 说明：`MarketColumnPlaceholder`、`UpcomingFeatures` 仍待真实数据替换。  
- 设计升级：v2.0 方案请查看 `Bloomberg_Frontend_Redesign.md`。

> 注：此文档仅作历史归档，实际实现应以当前规范（v2.0 黑色终端方案）为准。
